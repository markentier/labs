#ifndef WIFI_SSID
#error You must set WIFI_SSID env var (either in platformio.ini, .envrc, or in other means)
#endif
#ifndef WIFI_PW
#error You must set WIFI_PW env var (either in platformio.ini, .envrc, or in other means)
#endif
